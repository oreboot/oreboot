[alias]
xtask = "run --package xtask --"
make = "xtask make"
asm = "xtask asm"
flash = "xtask flash"
# debug = "xtask debug"
gdb = "xtask gdb"
analyze = "xtask analyze"

# todo: xtask qemu for emulation
