[package]
name = "uefi"
version = "0.1.0"
authors = ["Oreboot Authors"]
edition = "2021"

[dependencies]
cty = "0.2.1"

[build-dependencies]
bindgen = "0.58.1"

[profile.release]
opt-level = 'z'  # Optimize for size.
