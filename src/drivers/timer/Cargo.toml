[package]
name = "timer"
version = "0.1.0"
authors = ["Oreboot Authors"]
edition = "2021"

[profile.release]
opt-level = 'z'  # Optimize for size.

[dependencies]
model = { path = "../model"}
tock-registers = "0.7.0"
vcell = "0.1.2"

#[features]
#amdmmio = []
