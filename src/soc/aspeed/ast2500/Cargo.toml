[package]
name = "soc"
version = "0.1.0"
authors = ["Oreboot Authors"]
edition = "2018"

[profile.release]
opt-level = 'z'  # Optimize for size.

[dependencies]
register = "0.5.0"
cpu = { path = "../../../cpu/armltd/arm1176jz-s"}
