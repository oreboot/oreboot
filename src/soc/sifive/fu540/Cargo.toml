[package]
name = "soc"
version = "0.1.0"
authors = ["coreboot"]
edition = "2018"

[profile.release]
opt-level = 'z'  # Optimize for size.

[dependencies]
model = { path = "../../../drivers/model/"}
register = "0.3.2"

