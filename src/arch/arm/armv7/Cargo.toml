[package]
name = "arch"
version = "0.1.0"
authors = ["Oreboot Authors"]
edition = "2018"

[dependencies]
consts = { path="../../../lib/consts"}
model = { path = "../../../drivers/model" }

[profile.release]
opt-level = 'z'  # Optimize for size.
