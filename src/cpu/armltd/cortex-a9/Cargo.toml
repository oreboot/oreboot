[package]
name = "cpu"
version = "0.1.0"
authors = ["Oreboot Authors"]
edition = "2021"

[dependencies]
arch = { path = "../../../arch/arm/armv7"}

[profile.release]
opt-level = 'z'  # Optimize for size.
