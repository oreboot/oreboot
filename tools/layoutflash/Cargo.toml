[package]
name = "layoutflash"
version = "0.1.0"
authors = ["Ryan O'Leary <ryanoleary@google.com>"]
edition = "2021"

[dependencies]
clap = { version = "3", features = ["derive"] }
device_tree = { path = "../../src/lib/device_tree" }
oreboot-drivers = { path = "../../src/drivers" }
